# 🎨 首页背景图片使用说明

## ✅ 已完成的调整

### 1️⃣ "如何开始"改为横向布局 ✅
- 三个步骤从左到右排列
- 紧凑布局，用箭头连接
- 响应式：手机端自动变为垂直

### 2️⃣ 树状结构优化 - 平滑曲线 ✅
- 连接线改为渐变色（红色+金色）
- 添加圆角曲线效果
- 更自然的引导视觉效果

### 3️⃣ 背景图片和透明度 ✅
- CSS已配置背景图片路径
- 半透明遮罩层优化可读性
- 所有组件添加毛玻璃效果

---

## 📸 如何添加背景图片

### 方法1：放置图片文件

**步骤：**

1. **准备图片**
   - 图片名称：`首页背景.png`
   - 推荐尺寸：1920x1080 或更大
   - 格式：PNG 或 JPG

2. **放置位置**
   ```
   D:\JavaWeb\English-web\web\images\
   └── 首页背景.png  ← 放在这里
   ```

3. **刷新浏览器**
   - 按 `Ctrl + F5` 强制刷新
   - 背景图片将自动显示

---

### 方法2：如果图片在其他位置

**如果图片叫其他名字，例如 `background.jpg`**

编辑 `css/home.css` 第8行：

```css
/* 原代码 */
url('../images/首页背景.png') center/cover no-repeat;

/* 改为 */
url('../images/background.jpg') center/cover no-repeat;
```

---

### 方法3：使用网络图片

如果暂时没有本地图片，可以使用网络图片：

编辑 `css/home.css` 第8行：

```css
url('https://example.com/your-image.jpg') center/cover no-repeat;
```

---

## 🎨 当前设计特点

### Hero区域
- ✨ 半透明黑色遮罩（50-60%透明度）
- ✨ 标题：80px，白色，超大阴影
- ✨ 副标题：26px，白色，柔和阴影
- ✨ 字体加粗，字间距加大

### 树状结构
- ✨ 根节点：半透明红色渐变卡片
- ✨ 连接线：红色到金色渐变
- ✨ 分支卡片：95%白色透明，毛玻璃效果

### 如何开始
- ✨ 横向布局，5列（卡片-箭头-卡片-箭头-卡片）
- ✨ 半透明粉色背景
- ✨ 悬停动画效果

---

## 🔧 调整透明度

### 如果背景太暗，文字看不清

编辑 `css/home.css` 第7行：

```css
/* 原代码 - 50-60%黑色遮罩 */
linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.6)),

/* 改为更透明 - 30-40%黑色遮罩 */
linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.4)),
```

### 如果背景太亮，需要更深色

```css
/* 改为更深色 - 70-80%黑色遮罩 */
linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.8)),
```

---

## 🎯 建议的背景图片类型

### ✅ 推荐
- 中国传统建筑（故宫、长城）
- 书法、水墨画元素
- 红色为主色调的图片
- 模糊或柔和的背景

### ❌ 不推荐
- 过于复杂的图案
- 对比度太强的图片
- 文字太多的图片

---

## 📁 完整文件结构

```
D:\JavaWeb\English-web\web\
├── index.html              ← 已更新
├── css/
│   ├── style.css
│   └── home.css           ← 已更新（背景+样式）
└── images/
    └── 首页背景.png       ← 需要添加这个文件
```

---

## 🚀 测试步骤

1. **放置背景图片**
   - 将 `首页背景.png` 放到 `images/` 文件夹

2. **打开首页**
   - 双击 `index.html`

3. **检查效果**
   - ✅ 背景图片显示
   - ✅ 标题清晰可见
   - ✅ 树状结构连接线流畅
   - ✅ "如何开始"横向排列

4. **如果背景不显示**
   - 检查图片路径是否正确
   - 检查图片文件名是否一致
   - 按 `Ctrl + F5` 强制刷新
   - 按 `F12` 打开控制台查看错误

---

## 💡 备用方案

如果暂时没有背景图片，当前CSS已设置备用方案：

```css
background-color: #DC143C;  /* 纯红色背景 */
```

这样即使图片加载失败，也会显示中国红背景。

---

## 🎨 快速调整指南

### 调整标题大小
编辑 `css/home.css` 第24行：
```css
font-size: 80px;  /* 改为你想要的大小 */
```

### 调整标题颜色
编辑 `css/home.css` 第27行：
```css
color: white;  /* 改为其他颜色，如 #FFD700 金色 */
```

### 调整字间距
编辑 `css/home.css` 第29行：
```css
letter-spacing: 12px;  /* 改为更大或更小 */
```

---

**更新完成时间**：2026-08-16  
**版本**：v2.1  
**状态**：✅ 等待添加背景图片

需要添加图片后即可完美显示！🎉
